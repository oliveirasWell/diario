/** Shared styling for the native `<select>`s in the room panel (grade/section, subject). */
export const NATIVE_SELECT_CLASS_NAME =
  "h-8 bg-muted/40 px-2.5 text-sm outline-none focus-visible:ring-3 focus-visible:ring-ring/40";
